# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE
    - M2=TWO

build:

  ci:
    - ps -eax
    - ls -al

integrations:
  hub:
    - integrationName: ship-docker
      type: docker
      
